import React from 'react';
import Button from 'react-bootstrap/Button';

function ScoreSort(props){
    
    const scoresort = ()=>{
    }


    return (
        <div>
            <Button onClick={scoresort()}>Average Score</Button>
        </div>

    );
}

export default ScoreSort;