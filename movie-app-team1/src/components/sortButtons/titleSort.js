import React from 'react';
import Button from 'react-bootstrap/Button';

function TitleSort(props){
    return (
        <div>
            <Button>Title</Button>
        </div>

    );
}

export default TitleSort;